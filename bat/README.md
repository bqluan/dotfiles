# bat

To use this configuration, create a symlink from bat's config directory
to this directory.

By default, [bat](https://github.com/sharkdp/bat) looks for its configuration
in `~/.config/bat`.

Run this command to link it:

```bash
ln -s "$(pwd)" ~/.config/bat
```
