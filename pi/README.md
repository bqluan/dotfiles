# Pi

To use this configuration, create a symlink from Pi's config directory
to this directory.

By default, [Pi](https://github.com/badlogic/pi-mono) looks for its configuration
in `~/.pi`.

Run this command to link it:

```bash
ln -sf "$(pwd)" ~/.pi
```
